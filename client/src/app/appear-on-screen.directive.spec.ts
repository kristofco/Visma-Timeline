import { AppearOnScreenDirective } from './appear-on-screen.directive';

describe('AppearOnScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearOnScreenDirective();
    expect(directive).toBeTruthy();
  });
});
